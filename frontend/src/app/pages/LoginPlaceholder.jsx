export function LoginPlaceholder() {
  return (
    <main className="app-auth">
      <div className="app-panel relative z-[1] w-full max-w-lg p-6 sm:p-10">
        <p className="text-xs font-semibold uppercase tracking-[0.16em] text-text-muted">Sesión</p>
        <h1 className="mt-3 font-display text-3xl font-extrabold tracking-display text-navy">
          Iniciar sesión
        </h1>
        <p className="mt-3 text-base text-text-muted">
          La pantalla de acceso se porta en un commit siguiente.
        </p>
      </div>
    </main>
  );
}
